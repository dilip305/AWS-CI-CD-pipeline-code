buildspec.yml
version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands: [npm ci]
  pre_build:
    commands: [npm run lint, npm test]
  build:
    commands: [npm run build]
artifacts:
  files: ['**/*']
